---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
tags: [job-scheduling]
---

Using the Catalyst Node SDK, write the `cron().createCron()` call to create a Calendar-type cron that fires a Job function called `sendWeeklyDigest` every Monday at 9am. Show the exact `cron_type` value you'd pass.
