---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
tags: [appsail]
---

I'm on a Mac and I use Colima instead of Docker Desktop. Walk me through deploying my AppSail service as a Docker container from this machine — the docker builds work fine locally.
