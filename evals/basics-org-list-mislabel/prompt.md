---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
tags: [basics]
---

I ran `catalyst init --org 6xxxxx -ni` without -p and got an error that printed 'The following orgs are available:' followed by a list of names and IDs. But when I pass one of those IDs as --org it still fails. What do I do with that list?
