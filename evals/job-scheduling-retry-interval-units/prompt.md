---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
tags: [job-scheduling]
---

I'm submitting an immediate job via the Catalyst SDK and want it to retry every 15 minutes on failure, up to 3 retries. Write the `retry_interval` and `number_of_retries` values I should pass.
